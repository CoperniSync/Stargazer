using UnityEngine;
using ChargerAstronomyEngine;

public class TEMP : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        var test = new Class1();
        Debug.Log(test.Test());
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
